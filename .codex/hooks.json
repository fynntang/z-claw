{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | tr -d '\\r' | while read -r f; do case \"$f\" in *.rs) rustfmt --edition 2024 \"$f\" ;; esac; done 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Formatting with rustfmt..."
          }
        ]
      }
    ]
  }
}
